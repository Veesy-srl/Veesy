@model Veesy.Presentation.Model.Auth.ResetPasswordViewModel 
@{
    ViewData["Title"] = "Reset Password";
}

    <div class="vertical-center text-center bg-grey-veesy bg-auth">
        <div class="container">
            <div class="row row-login-padding">
                <div class="col-md-6 offset-md-3">
                    <form method="post">
                        @Html.HiddenFor(s => s.Email)
                        @Html.HiddenFor(s => s.Token)
                        <h1 class="h3 mb-2 mt-4 pt-5 mt-md-3 pt-md-0 fw-normal text-color" >Choose a new password!</h1>
                        <h5 class="mt-3 mb-3 size-custom text-color">Please, enter your new password.</h5>

                        <div style="height: 20px;"></div>
                        <div class="input-group">
                            <label class="fw-border size-custom text-color">Password*</label>
                            <input asp-for="Password" class="input--style-1 mt-2 text-profile" type="password">
                        </div>
                        <div class="input-group">
                            <label class="fw-border size-custom text-color">Confirm password*</label>
                            <input asp-for="PasswordConfirm" class="input--style-1 mt-2 text-profile" type="password">
                        </div>

                        <button class="a-font-p w-100 mt-2 rounded-pill btn btn-primary signup" type="submit">change password</button>
                    </form>
                </div>
            </div>
        </div>
    </div>